arch:
  - amd64
os: linux
dist: jammy
git:
  depth: false

language: bash

services:
  - docker

before_install:
  - docker build --build-arg GH_TOKEN=$GITHUB_TOKEN -t moussavdb/application-retro-pxl_server:latest .
