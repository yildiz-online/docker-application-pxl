arch:
  - amd64
os: linux
dist: jammy
git:
  depth: false

language: bash

services:
  - docker

before_install:
  - docker build --build-arg GH_TOKEN=$GITHUB_TOKEN --build-arg=nexus_retro_password=$nexus_retro_password --build-arg=test=$test -t moussavdb/application-retro-pxl_server:latest .
